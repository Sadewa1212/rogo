import { Uri, FakeAuth, FakeLoading } from "./type"
import { callAPI } from './api'
export type {
    Uri,
    callAPI,
    FakeAuth,
    FakeLoading,
}

export {
    firebaseApp,
} from "./firebase"
